package com.core.api.mcpe.entity;

import com.core.api.engine.PointerClass;

public class Player extends Mob {
    public Player(long ptr) {
        super(ptr);
    }
    public Player(PointerClass ptr) {
        super(ptr);
    }
}
